# Optional. Only needed when the frontend is served from a different origin than the API.
# Leave unset for local dev (Vite proxy forwards /api to backend).
# Example for production: https://your-backend.com
# VITE_API_URL=https://your-backend.com
